/**
 *
 * @author : wangrn
 * 
 * 改进的朴素贝叶斯算法, 结合词在类别中的权重, 进行适当的调权.
 * 
 * 
 * Pr(c|d) 是文档d属于类c的概率. 假定文档内词是独立的, 则Pr(c|d) = Σ Pr(c|w)*Pr(w|d) .
 * 
 *
 */

int main() 
{
    printf ("Hello world!\n");
    return 0;
}
